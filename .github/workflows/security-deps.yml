# GENERATED by UDAP Build Project from .udap/pipeline.yaml — do not
# edit by hand; change the spec and re-render.
name: security-deps
'on':
  workflow_dispatch: {}
jobs:
  dependency_scan:
    runs-on: ubuntu-latest
    timeout-minutes: 180
    steps:
    - uses: actions/checkout@v4
    - name: Set up JDK 21
      uses: actions/setup-java@v4
      with:
        distribution: temurin
        java-version: '21'
        cache: maven
    - name: Restore the NVD vulnerability database
      uses: actions/cache@v4
      with:
        path: ~/.dependency-check-data
        key: nvd-db-${{ github.run_id }}
        restore-keys: 'nvd-db-

          '
    - name: OWASP dependency vulnerability scan (reporting mode)
      run: bash scripts/dependency-scan.sh
      env:
        NVD_API_KEY: ${{ secrets.NVD_API_KEY }}
    - name: Upload dependency-check report
      uses: actions/upload-artifact@v4
      with:
        name: dependency-check-report
        path: 'application/target/dependency-check-report.html

          application/target/dependency-check-report.json

          '
        retention-days: 30
        if-no-files-found: warn
